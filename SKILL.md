---
name: ws2812b-strip-control
description: 在 ESP32 / ESP32-S3 上把 WS2812B（NeoPixel）灯带做成一套可控的灯效系统：接线与供电、固件烧录、本地网页控制台，以及"不亮 / 只亮前几颗 / 颜色不对 / 闪烁"这类故障的分层定位。用户提到灯带、灯珠、WS2812、幻彩灯带、RGB 灯带、跑马灯的控制或排查时使用。
metadata:
  short-description: 驱动和排查 ESP32 上的 WS2812B 灯带
---

# WS2812B 灯带控制

从"刚焊好线、灯不亮"到"能远程调灯效"的完整路子。

## 先认清失败模式

这类任务翻车基本不是灯效代码写错，而是**在看不见板子状态的情况下反复改代码**。顺序永远是：

1. 让板子**能说话**（串口有日志）
2. 把"说话"升级成**能远程看、能远程发指令**
3. 才去动灯带

`assets/` 里就是第 2 步的现成件：

- `strip_console.py` —— 把串口包成 `http://127.0.0.1:877x` 的本地服务 + 网页
- `strip_ui.html` —— 配套中文网页（灯珠数按长度算 / 亮度 / 单色 / 灯效 / 呼吸快慢 / 用电上限）。
  里面所有数值型设置都是**改完即自动保存**（无保存按钮），契合"用户会突然断电"这个前提。

拷进工程目录，`python3 strip_console.py` 即可。

参考固件在 `firmware/`（ESP32-S3、数据脚 GPIO 4、N16R8），可直接编译烧录，用来起步或对照。
**协议是稳定的那层，引脚和灯珠数是每台机器自己的事**——换板子照着
`references/board-protocol.md` 的接口改，不用重写控制台。

**为什么要这层包装**：烧录和 `pio device monitor` 会独占串口，反复开关很痛苦；包成 HTTP 之后，烧录前 `curl .../api/quit` 放开串口、烧完再拉起，全程不用打断用户。

几个要记住的点：

- 端口在 `8770~8790` 里**自动找空位**，实际端口写在 `logs/console_port.txt`；发命令前先读它，别假设一定是 8770。
- 如果已经有一个实例在跑，它**不会重复启动**，而是直接把浏览器指过去（两个一起开会抢串口）。所以想开第二个必须显式传串口，例如 `python3 strip_console.py /dev/cu.usbmodem14301`。
- 依赖 `pyserial`。报"这台电脑缺 pyserial"就是没装（虚拟环境里也要装）。

> 这两个文件与固件的**回话格式绑定**（心跳行、`>> 效果：` 之类的应答）。换固件要同步改 `strip_console.py` 里的 `*_RE` 解析规则，否则界面状态不会动。

## 执行步骤

1. **最小固件 + 串口日志**。`-DARDUINO_USB_CDC_ON_BOOT=1` 让日志走 USB 口；这一步只验证"芯片在跑、能打印"。
2. **包成控制台**。拷 `assets/` 两个文件，跑起来。
3. **单颗验证时序**。先只驱动 1 颗跑固定色，再接整条。
4. **填对灯珠数**。按 `长度 × 每米颗数` 估算，再让用户微调——别让用户自己数灯珠。灯珠数用命令改，不要重新编译。
5. **算电流、定供电**（见硬约束 1）。
6. 最后才是灯效和交互。

## 三条硬约束

违反任意一条，都会产生**看起来像代码 bug** 的现象。

1. **5V 灯带 + 高亮度 + 一定长度 → 必须独立 5V 供电，且和开发板共地。**
   开发板从 USB 取的那点电带不动。典型现象："USB 一拔灯效就停"、"高亮度时随机闪"。
   短灯带（≤ 24 颗、≤ 1.4A）可以直接从开发板 5V 脚取电。
2. **3.3V 数据电平对 5V 供电的 WS2812B 是临界值**（VIH ≈ 0.7 × VDD ≈ 3.5V）。
   短线通常能直接驱动；出现随机闪白 / 闪绿就补 74AHCT125，别先去改代码。
3. **所有可调参数必须写进 NVS（Preferences）**，否则每次断电回出厂值，用户会认为"程序坏了"。
   一条命令改一个值就存一次。
   **例外要想清楚：「关灯」不要存。** 它和"开机默认灯效"抢同一格存储，存了就变成
   "点过关灯 → 拔电 → 再也不亮"。把 `fx` 定义成"下次上电放哪个"，关灯只改 RAM 里的当前状态。

## 用"排查灯效"把问题分层

不要猜。固件里常驻这几个**画面固定、逻辑极简**的灯效，这是整套方案最值钱的部分：

- **参考灯**：前 4 颗固定红绿蓝白 —— 判断颜色顺序和基本通路
- **斑马纹**：画面完全静止 —— 判断"闪烁"是数据问题还是动画本身
- **单颗定位**：只亮第 N 颗 —— 判断"只亮前几颗"卡在哪一颗

完整对照表（现象 → 归属层 → 验证手法 → 处置）见 [references/diagnosis.md](references/diagnosis.md)。

## 症状速查

| 现象 | 八成是 | 先做这个 |
| --- | --- | --- |
| 板载小灯在跑，灯带全黑 | 供电 / 共地 / 数据线 | 量灯带的 5V 与 GND；确认 DIN |
| 只亮前几颗（6、8 颗之类） | 数据没走完：速率太快 / 帧头不够 / 信号完整性 | 降速、加帧头、改整帧直发 |
| 全亮但颜色不对 | 位序（GRB/RGB）或时序 | 发纯红，盯第一颗 |
| 第一颗颜色不对 | 首颗采样点被前导干扰 | 开"首颗启动缓冲" + 调帧头 |
| 复杂灯效闪、单色正常 | 每帧写入长度不一致 / 帧率抖动 | 用斑马纹对照 |
| 中间某几颗不定时亮起 | 帧内缝隙被灯珠当成锁存 | 换整帧直发 |
| 手机连不上热点 | 手机自动切回蜂窝 | 关掉蜂窝 / 自动切换 |

成因、诱因和踩坑清单见 [references/pitfalls.md](references/pitfalls.md)。

## 同任务的其它方案

| 想要 | 选它 | 代价 |
| --- | --- | --- |
| 任意灯效、完全可控、能随时改 | 自研固件 + 本地控制台（本 skill） | 灯效要自己写 |
| 最快出效果、手机 App、生态现成 | WLED | 固件是黑盒；烧录和分区表有坑；自定义受限 |
| 只想有几十种现成效果 | 成品控制器（SP107E 之类） | 不可编程、不可远程 |
| 接 Home Assistant / MQTT | WLED，或自研 + MQTT | 多一层服务要维护 |

选 WLED 前先读 [references/pitfalls.md](references/pitfalls.md) 里"分区表"那条：它造成的无限重启会被误判成"程序正常在跑"。

## 操作细节

串口命令表、灯效编号、参数含义、持久化键名、电流估算：见 [references/board-protocol.md](references/board-protocol.md)。
